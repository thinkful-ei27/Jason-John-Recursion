'use strict';

// List all permutations of anagrams for a string
// What is the input? A string
// What is the output? 
// What is the input to each recursive call? 
// What is the output of each recursive call? 
const anagram = (str) => {

  // Base case
  
  
};

console.log(anagram('east'));